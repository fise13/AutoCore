import CoreGraphics

/// Консистентные отступы по сетке 8pt (re-exports from DesignSystem for backward compatibility)
enum Spacing {
    static let unit: CGFloat = DSSpacing.unit
    static let x2: CGFloat = DSSpacing.x2
    static let x3: CGFloat = DSSpacing.x3
    static let x4: CGFloat = DSSpacing.x4
}
